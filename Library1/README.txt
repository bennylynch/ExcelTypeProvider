This is a type provider for simple 'tables' in excel starting in cell A1 with a header row.

This is a fork of the Fsharpx ExcelTypeProvider, modified to use ClosedXML if poss.
It will only use office interop if absolutely necessary.

